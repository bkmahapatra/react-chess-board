import React from 'react'

const Chessboard = () => {
    const boardSize = 8;

    const board = Array.fill(boardSize).map;

    console.log(board);

    return (
        <div><li><li></li></li></div>
    )
}

export default Chessboard